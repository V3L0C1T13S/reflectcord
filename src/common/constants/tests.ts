import dotenv from "dotenv";

dotenv.config();

export const TestServerId = process.env["TEST_SERVER_ID"];

export const TestChannelId = process.env["TEST_CHANNEL_ID"];

export const TestUserId = process.env["TEST_USER_ID"];
