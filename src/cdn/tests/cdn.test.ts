import { describe, expect, test } from "@jest/globals";
import { baseURL, reflectcordCDNURL, urlScheme } from "../../common/constants";

const cdnURL = `${urlScheme}://${reflectcordCDNURL}`;

describe("cdn get requests", () => {
  test("get image", async () => {
  });
});
